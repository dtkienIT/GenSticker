# macOS-only Core ML conversion environment. Keep separate from the Android
# ONNX exporter because coremltools does not support the project's Python 3.13 stack.
accelerate==0.33.0
coremltools==8.0
diffusers[torch]==0.30.2
huggingface-hub==0.24.6
peft==0.12.0
safetensors==0.4.5
scikit-learn==1.5.1
torch==2.4.1
transformers==4.44.2
git+https://github.com/apple/ml-stable-diffusion.git@1.1.1

